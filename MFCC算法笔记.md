# MFCC 算法笔记

## 1. MFCC 是什么

MFCC 全称是 `Mel-Frequency Cepstral Coefficients`，中文常译为“梅尔频率倒谱系数”。

它的目标不是保留原始波形本身，而是提取一段短时语音的频谱包络特征。  
在语音任务里，MFCC 常被当作“每一小段语音的紧凑表示”。

## 2. 为什么要分帧

语音信号整体上是时变的，但在很短的时间内可以近似看成平稳信号。  
所以 MFCC 不是对整段音频一次性计算，而是先切成很多短帧，再逐帧提特征。

常见流程是：

1. 读取原始波形
2. 分帧并加窗
3. 对每帧做频谱分析
4. 映射到 Mel 频率尺度
5. 取对数
6. 做 DCT，得到 MFCC

## 3. MFCC 的计算流程

### 第一步：预加重和分帧

很多教材会先做预加重，但现代工具库不一定默认做。  
真正关键的是分帧：把长语音切成很多短时间窗口。

例如本项目中：

- `sample_rate = 22050`
- `win_length = 1024`
- `hop_length = 256`

它表示：

- 每帧窗口长度是 `1024` 个采样点
- 相邻两帧之间前进 `256` 个采样点

换成时间大约是：

- 每帧长度：`1024 / 22050 ≈ 46.4 ms`
- 帧移步长：`256 / 22050 ≈ 11.6 ms`

所以一段 `3` 秒语音，大致会得到：

`3 × 22050 / 256 ≈ 258` 帧


### 第二步：加窗

每一帧通常会乘一个窗函数，常见是 Hann 窗或 Hamming 窗。  
目的很简单：减少分帧截断带来的频谱泄漏。

你可以把它理解成：

- 不让帧的边缘太突兀
- 让频谱估计更稳定

### 第三步：短时傅里叶变换

对每一帧做 FFT，把时域信号变成频域表示。  
这样我们就能看到该帧在不同频率上的能量分布。

在本项目里，`n_fft = 1024`，所以每帧会先变成一组频谱系数，再进一步处理。

### 第四步：功率谱

频谱本身是复数，实际做 MFCC 时通常更关心能量大小。  
所以会把频谱转成功率谱或幅度谱。

此时得到的是：

- 横轴：频率
- 数值：该频率上的能量

### 第五步：Mel 滤波器组

人耳对频率的感知并不是线性的。  
低频分辨更细，高频分辨更粗。

MFCC 用 Mel 滤波器组把线性频率轴压到更符合听觉习惯的 Mel 尺度上。  
做法是拿一组重叠的三角滤波器去“汇总”不同频带的能量。

这一层之后，特征不再是“每个 FFT 频点的能量”，而是“每个 Mel 频带的能量”。

直觉上，这一步是在做两件事：

- 降维
- 让频率表示更接近听觉感知

### 第六步：取对数

对 Mel 频带能量取对数，原因主要有两个：

- 人耳对响度的感知更接近对数关系
- 把乘性变化转成加性变化，后续更稳定

所以这一步之后得到的是 `log-Mel` 能量。

### 第七步：DCT

最后对 `log-Mel` 能量做离散余弦变换（DCT），得到 MFCC。

这样做的作用是：

- 去相关
- 让主要信息集中到前几个系数

因此 MFCC 通常只保留前若干维，比如 `13` 维、`20` 维、`40` 维。

本项目里使用的是：

- `n_mfcc = 40`

所以每一帧最后得到一个 `40` 维向量。

## 4. MFCC 的输出形状

如果一条音频被分成 `T` 帧，那么 MFCC 输出通常可以写成：

`[n_mfcc, T]`

在本项目里就是：

`[40, T]`

含义是：

- 一共有 `T` 个时间帧
- 每个时间帧对应一个 `40` 维 MFCC 向量

例如某条音频如果有 `258` 帧，那么它的 MFCC 形状就是：

`[40, 258]`

## 5. 本项目里 MFCC 是怎么用的

代码在 [src/ser_reservoir/data.py](/g:/Izhikevich%20Reservoir%20for%20Speech%20Emotion%20Projection/src/ser_reservoir/data.py#L100)：

```python
mfcc = librosa.feature.mfcc(
    y=waveform,
    sr=sr,
    n_mfcc=cfg.n_mfcc,
    n_fft=cfg.n_fft,
    hop_length=cfg.hop_length,
    win_length=cfg.win_length,
)
```

提取之后又做了一步按行归一化：

```python
row_min = mfcc.min(axis=1, keepdims=True)
row_max = mfcc.max(axis=1, keepdims=True)
mfcc = (mfcc - row_min) / max(row_max - row_min, 1e-8)
```

这意味着：

- 对每个 MFCC 维度，沿时间轴做最小值-最大值归一化
- 把每一维都缩放到大致 `0` 到 `1`

这样更适合后续送入储备池，或者作为直接分类特征。

## 6. 这个项目里“一条音频的特征”有两种情况

### 情况 A：送入储备池

输入给储备池的是整个 MFCC 序列，也就是 `[40, T]`。  
储备池按帧读取，每次取一个 `40` 维向量作为当前输入。

### 情况 B：direct MFCC 分类实验

在不经过储备池的实验里，会先把每条音频补到统一帧长，再直接展平。

如果统一到 `258` 帧，那么最终一条音频的向量维度就是：

`40 × 258 = 10320`

