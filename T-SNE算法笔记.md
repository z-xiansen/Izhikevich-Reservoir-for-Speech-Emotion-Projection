# t-SNE 算法笔记

## 1. t-SNE 是什么
t-SNE（t-Distributed Stochastic Neighbor Embedding）是一种**非线性降维算法**，常用于把高维特征映射到 2 维或 3 维做可视化。

它的核心目标不是精确保留所有距离，而是：

**让高维空间中的近邻点，在低维空间中仍然彼此靠近。**

因此，t-SNE 更关注**局部结构**，不强调全局几何关系。

一句话总结：**t-SNE 先把高维和低维中的样本关系都写成概率分布，再通过最小化 $KL(P\|Q)$ 来调整低维坐标，使高维近邻在低维中尽量保持接近；它擅长保留局部结构，但不擅长表达全局距离。**

---

## 2. 基本思想
设高维样本为：

$$
x_1, x_2, \dots, x_N \in \mathbb{R}^D
$$

希望找到低维表示：

$$
y_1, y_2, \dots, y_N \in \mathbb{R}^d,\quad d=2 \text{ 或 } 3
$$

t-SNE 的做法可以概括为三步：

1. 在高维空间中，把“距离”转成“相似度概率”。
2. 在低维空间中，再定义一套相似度概率。
3. 调整低维点的位置，使两套概率分布尽量一致。

也就是说，t-SNE 比较的不是原始距离本身，而是**邻域关系的概率分布**。

---

## 3. 高维空间中的相似度
### 3.1 条件概率
对每个点 $x_i$，以它为中心建立一个高斯分布，定义点 $x_j$ 是它邻居的条件概率：

$$
p_{j|i} = \frac{\exp\left(-\|x_i-x_j\|^2 / 2\sigma_i^2\right)}
{\sum_{k\neq i}\exp\left(-\|x_i-x_k\|^2 / 2\sigma_i^2\right)}, \quad p_{i|i}=0
$$

含义：

- 如果 $x_j$ 离 $x_i$ 很近，那么 $p_{j|i}$ 大。
- 如果 $x_j$ 离 $x_i$ 很远，那么 $p_{j|i}$ 小。

这里的 $\sigma_i$ 不是固定常数，而是针对每个样本单独调整。

最终得到一个对称矩阵 $P=[p_{ij}]$

### 3.2 Perplexity（重要参数）
t-SNE 用 `perplexity` 来控制每个点“关注多少个邻居”，它与分布熵有关：

$$
\text{Perp}(P_i) = 2^{H(P_i)}
$$

其中

$$
H(P_i) = -\sum_j p_{j|i}\log_2 p_{j|i}
$$

直观上可以把 `perplexity` 理解为：

**每个点有效邻居数的大致规模。**

常见经验：

- 样本较少时，取 5 到 30。
- 样本较多时，可以尝试 30 到 50。

---

## 4. 低维空间中的相似度
在低维空间中，t-SNE 不再用高斯分布，而是使用**自由度为 1 的 Student t 分布**：

$$
q_{ij} = \frac{(1+\|y_i-y_j\|^2)^{-1}}
{\sum_{k\neq l}(1+\|y_k-y_l\|^2)^{-1}}, \quad q_{ii}=0
$$

最终得到低维分布 $Q=[q_{ij}]$。

### 为什么不用高斯，而用 t 分布？
这是 t-SNE 最关键的设计之一。

如果低维空间也使用高斯分布，会出现**拥挤问题（crowding problem）**：

- 高维空间中，很多点可以彼此分开。
- 映射到二维后，平面空间太小，很多点会挤在一起。

t 分布具有**重尾（heavy tail）**性质，远距离点之间仍保留较明显的斥力，因此：

- 同一簇内部能聚得比较紧。
- 不同簇之间更容易被拉开。

这也是为什么 t-SNE 的可视化图常常会出现比较清晰的团块结构。

---

## 5. 优化目标：KL 散度
t-SNE 希望低维分布 $Q$ 尽可能接近高维分布 $P$，因此定义损失函数：

$$
C = KL(P\|Q)=\sum_i\sum_j p_{ij}\log\frac{p_{ij}}{q_{ij}}
$$

这个目标有一个很重要的性质：

- 当高维里两点本来很近（$p_{ij}$ 大），但低维里被放得很远（$q_{ij}$ 小）时，惩罚很大。
- 当高维里两点本来不近，但低维里偶然靠近时，惩罚相对没那么强。

所以 t-SNE 的偏好非常明确：

**优先保住“近邻关系”，而不是严格保留所有成对距离。**

---

## 6. 梯度与直观解释
对低维坐标 $y_i$ 求梯度，可得到：

$$
\frac{\partial C}{\partial y_i}
=4\sum_j (p_{ij}-q_{ij})(y_i-y_j)(1+\|y_i-y_j\|^2)^{-1}
$$

这个公式可以理解为两种“力”的共同作用：

### 6.1 引力
当

$$
p_{ij} > q_{ij}
$$

说明这对点在高维中应该更近，但在低维里离得还不够近，于是会产生**吸引作用**，把它们拉近。

### 6.2 斥力
当

$$
q_{ij} > p_{ij}
$$

说明这对点在低维里靠得太近了，但高维中并不是近邻，于是会产生**排斥作用**，把它们推开。

因此，t-SNE 的优化过程可以看成：

**近邻之间相互吸引，非近邻之间相互排斥。**

---

## 7. 参数理解
### 7.1 Perplexity
决定局部邻域尺度。

- 小 `perplexity`：更关注很局部的结构。
- 大 `perplexity`：会考虑更大范围的邻域关系。

### 7.2 学习率
学习率太小，优化慢且结构可能挤在一起；太大则可能导致分布异常发散。

### 7.3 随机种子
不同种子可能导致图整体旋转、翻转，甚至局部布局略变，但如果结构稳定，主要簇形态通常相似。
